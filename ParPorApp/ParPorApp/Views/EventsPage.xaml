<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ParPorApp.Views.EventsPage">
    <StackLayout>
        <StackLayout Orientation="Horizontal"/>
        <ListView x:Name="eventsListView">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextCell Text="{Binding Description}" TextColor="Black" />
                    <TextCell Text="{Binding StartDateTime}" TextColor="Black" />
                    <TextCell Text="{Binding EndDateTime}" TextColor="Black" />
                    <TextCell Text="{Binding LocationId}" TextColor="Black" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>
</ContentPage>